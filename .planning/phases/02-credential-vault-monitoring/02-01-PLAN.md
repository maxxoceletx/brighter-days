---
phase: 02-credential-vault-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-credential-vault-monitoring/credential-schema.sql
  - .planning/phases/02-credential-vault-monitoring/credential-seed.sql
  - .planning/phases/02-credential-vault-monitoring/1password-vault-spec.md
autonomous: true
requirements: [CRED-01, CRED-02]

must_haves:
  truths:
    - "A Supabase credentials table exists that extends Phase 1 compliance_items with renewal metadata, expiry tracking, and 1Password cross-references"
    - "Every known credential (CA license, DEA, NPI, CAQH, malpractice, business license, Medicare PTANs, Medi-Cal) has a seed data row with real numbers and dates"
    - "A 1Password vault spec tells Valentina exactly which logins go where, organized by functional use"
    - "The credentials table links to Phase 1 compliance_items via foreign key — no data duplication"
  artifacts:
    - path: ".planning/phases/02-credential-vault-monitoring/credential-schema.sql"
      provides: "DDL for credentials table, payer_tracker table, credential_alert_queue view, payer_credentialing_alerts view"
      contains: "CREATE TABLE credentials"
    - path: ".planning/phases/02-credential-vault-monitoring/credential-seed.sql"
      provides: "INSERT statements for all known credentials with real numbers from Phase 1 document audit"
      contains: "INSERT INTO credentials"
    - path: ".planning/phases/02-credential-vault-monitoring/1password-vault-spec.md"
      provides: "Complete 1Password vault structure spec with category organization, item list, and migration instructions"
      contains: "Brighter Days Practice"
  key_links:
    - from: "credential-seed.sql"
      to: "compliance_items"
      via: "compliance_item_id foreign key"
      pattern: "compliance_item_id.*FROM compliance_items"
    - from: "credential-schema.sql"
      to: "compliance-schema.sql"
      via: "REFERENCES compliance_items(id)"
      pattern: "REFERENCES compliance_items"
---

<objective>
Create the credential data foundation: Supabase schema extending Phase 1 with a credentials table and payer_tracker table, seed all known credentials with real data from Phase 1 document audit, and produce the 1Password vault structure spec.

Purpose: CRED-01 (organized vault) and CRED-02 (credential inventory) require a structured data store and a vault organization plan. This plan creates both the database schema and the vault spec that Valentina uses to organize all practice logins.

Output: credential-schema.sql (DDL), credential-seed.sql (seed data for ~12 credentials), 1password-vault-spec.md (vault organization guide for Valentina)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-credential-vault-monitoring/02-CONTEXT.md
@.planning/phases/02-credential-vault-monitoring/02-RESEARCH.md
@.planning/phases/01-compliance-audit-verification/compliance-schema.sql
@.planning/phases/01-compliance-audit-verification/01-01-SUMMARY.md

<interfaces>
<!-- Phase 1 schema that Phase 2 extends. The credentials table FK references compliance_items(id). -->

From .planning/phases/01-compliance-audit-verification/compliance-schema.sql:
```sql
CREATE TABLE compliance_items (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  req_id          TEXT NOT NULL UNIQUE,
  category        TEXT NOT NULL,
  title           TEXT NOT NULL,
  severity        TEXT NOT NULL,
  status          TEXT NOT NULL,
  description     TEXT,
  legal_citation  TEXT,
  verified_date   DATE,
  expiry_date     DATE,
  document_ref    TEXT,
  notes           TEXT,
  updated_at      TIMESTAMPTZ DEFAULT now()
);
```

From Phase 1 seed data — compliance_items rows that will be FK-referenced:
- req_id 'COMP-08' = CA Medical License row
- req_id 'COMP-10' = DEA Registration row
- req_id 'COMP-09' = Malpractice Insurance row
- req_id 'COMP-04' = Expiry tracking row (covers multiple credentials)

From 02-RESEARCH.md — known credential data:
- CA Medical License: A-177690, ACTIVE, expires 2028-06-30, 2-year cycle
- DEA Registration: FP3833933, ACTIVE, expires 2027-03-31, 3-year cycle
- ABPN Psychiatry Cert: #83742, ACTIVE, no expiry (CC)
- ABPN Child & Adolescent Cert: #13399, ACTIVE, no expiry (CC)
- CAQH ProView: ID 16149210, 120-day cycle, last attestation date UNKNOWN
- Malpractice (CAP/MPT): Policy #48289, ACTIVE, expires 2026-12-31, annual
- S-Corp Entity Coverage: #10709, ACTIVE, expires 2026-12-31, annual
- Business License (Torrance): BL-LIC-051057, EXPIRED, was 2025-12-31, annual
- Medicare PTAN (Group): CB496693, ACTIVE, no expiry, 5-year re-enrollment
- Medicare PTAN (Individual): CB496694, ACTIVE, no expiry, 5-year re-enrollment
- Medi-Cal PAVE: 100517999, ACTIVE, annual re-enrollment
- Individual NPI: 1023579513
- Group NPI: 1699504282
- EIN: 99-1529764
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase credential schema and seed all known credentials</name>
  <files>
    .planning/phases/02-credential-vault-monitoring/credential-schema.sql
    .planning/phases/02-credential-vault-monitoring/credential-seed.sql
  </files>
  <action>
Create two SQL files:

**credential-schema.sql** — DDL for the Phase 2 schema extension. Include exactly:
1. `credentials` table with all fields from 02-RESEARCH.md schema design (id, compliance_item_id FK to compliance_items, credential_type, credential_name, issuing_body, credential_number, holder, status, issue_date, expiry_date, renewal_cycle_days, renewal_cost_usd, renewal_portal_url, renewal_notes, alert_90/60/30/7 booleans, vault_entry_ref, document_ref, notes, updated_at). Include the 4 indexes from research.
2. `payer_tracker` table with all fields from 02-RESEARCH.md schema design (id, payer_name, payer_short, tebra_payer_id, network_type, contract_status, credentialing_status, credentialing_date, contract_effective_date, recred_due_date, recred_cycle_years, recred_is_estimated, can_bill, billing_notes, claim_submission_method, timely_filing_days, timely_filing_is_estimated, denial_patterns, portal_url, portal_login_ref, provider_relations_phone, provider_relations_email, credentialing_rep_name, credentialing_rep_email, fee_schedule_notes, payer_id_for_claims, npi_on_file, group_npi_on_file, ein_on_file, key_issues, contact_notes, updated_at). Include the 3 indexes from research.
3. `credential_alert_queue` view — exactly as specified in 02-RESEARCH.md (days_until_expiry, alert_level, display_color).
4. `payer_credentialing_alerts` view — exactly as specified in 02-RESEARCH.md (days_until_recred, recred_urgency).

Add header comment block matching Phase 1 style. Note these tables extend the Phase 1 compliance schema.

**credential-seed.sql** — INSERT statements for ALL known credentials:
1. CA Medical License A-177690 — link to compliance_items via subselect on req_id 'COMP-08'. Status ACTIVE, expiry 2028-06-30, renewal_cycle_days 730, portal https://www.breeze.ca.gov/, vault_entry_ref 'CA BreEZe (Medical Board)'.
2. DEA Registration FP3833933 — link to compliance_items req_id 'COMP-10'. Status ACTIVE, expiry 2027-03-31, renewal_cycle_days 1095, portal https://www.deaecom.gov/, vault_entry_ref 'DEA Diversion Control Portal'. Add renewal_notes about address mismatch (Walnut Creek vs Torrance — per Phase 1 finding).
3. ABPN Psychiatry Certification #83742 — no compliance_item_id link. Status ACTIVE, no expiry_date (NULL), credential_type 'certification'. Alert booleans all false (no expiry to alert on).
4. ABPN Child & Adolescent Certification #13399 — same pattern as #3.
5. CAQH ProView Attestation (CAQH ID 16149210) — Status ACTIVE, renewal_cycle_days 120. expiry_date = NULL with notes 'UPDATE REQUIRED: Valentina must verify last attestation date at proview.caqh.org — expiry = last_attestation + 120 days'. All alert booleans TRUE (highest-risk credential).
6. Malpractice Insurance CAP/MPT Policy #48289 — link to compliance_items req_id 'COMP-09'. Status ACTIVE, expiry 2026-12-31, renewal_cycle_days 365, portal https://www.cap-mpt.com/.
7. S-Corp Entity Coverage #10709 — Status ACTIVE, expiry 2026-12-31, renewal_cycle_days 365, holder 'entity'.
8. Business License Torrance BL-LIC-051057 — Status EXPIRED, expiry 2025-12-31, renewal_cycle_days 365, portal https://www.torranceca.gov/. Notes: 'EXPIRED — renewal required immediately.'
9. CA Statement of Information — Status PENDING, credential_type 'corporate_filing', portal https://bizfileplus.sos.ca.gov/, renewal_cycle_days 365.
10. Medicare PTAN Group CB496693 — Status ACTIVE per CONTEXT.md correction (NOT DEACTIVATED). credential_type 'payer_id', renewal_cycle_days 1825 (5 years), portal https://pecos.cms.hhs.gov/. Notes: 'Status corrected per Phase 2 discussion — Valentina confirmed ACTIVE enrollment 2026-02-27.'
11. Medicare PTAN Individual CB496694 — Same pattern as #10.
12. Medi-Cal PAVE 100517999 — Status ACTIVE, credential_type 'payer_id', renewal_cycle_days 365, portal https://www.dhcs.ca.gov/.

Use the subselect pattern from research: `(SELECT id FROM compliance_items WHERE req_id = 'COMP-XX' LIMIT 1)` for compliance_item_id links. For credentials with no Phase 1 compliance_items row, set compliance_item_id to NULL.

CRITICAL: Medicare rows MUST show status = 'ACTIVE' and can_bill = true per locked CONTEXT.md decision. Phase 1 flagged as DEACTIVATED but this is incorrect per Valentina's correction.

holder = 'valentina' for personal credentials, 'entity' for corporate credentials.
  </action>
  <verify>
Both SQL files exist and are valid:
- `grep -c "INSERT INTO credentials" .planning/phases/02-credential-vault-monitoring/credential-seed.sql` returns 12 (one per credential)
- `grep -c "CREATE TABLE" .planning/phases/02-credential-vault-monitoring/credential-schema.sql` returns 2 (credentials + payer_tracker)
- `grep -c "CREATE VIEW" .planning/phases/02-credential-vault-monitoring/credential-schema.sql` returns 2 (credential_alert_queue + payer_credentialing_alerts)
- `grep "REFERENCES compliance_items" .planning/phases/02-credential-vault-monitoring/credential-schema.sql` confirms FK link
- `grep "ACTIVE" .planning/phases/02-credential-vault-monitoring/credential-seed.sql | grep -i "medicare"` confirms Medicare is ACTIVE (not DEACTIVATED)
  </verify>
  <done>credential-schema.sql contains both new tables and both views with proper indexes. credential-seed.sql contains 12 INSERT statements using real credential numbers from Phase 1 audit, with compliance_item_id FK links where applicable. Medicare shows ACTIVE status per CONTEXT.md correction. CAQH has 120-day cycle with note about verifying last attestation date.</done>
</task>

<task type="auto">
  <name>Task 2: Create 1Password vault structure spec with migration instructions</name>
  <files>
    .planning/phases/02-credential-vault-monitoring/1password-vault-spec.md
  </files>
  <action>
Create a professional, actionable document that Valentina and Maxi can follow step-by-step to organize 1Password. Structure:

**Header section:**
- Title: "1Password Vault Organization — Brighter Days Practice"
- Practice identifiers: NPI 1023579513, Group NPI 1699504282, EIN 99-1529764, CAQH 16149210
- Vault name: "Brighter Days Practice" (shared vault)
- Access: Both Maxi and Valentina have full access

**Section 1: Vault Category Structure**
Use the exact functional grouping from 02-RESEARCH.md Standard Stack section:
- EHR & Billing (Tebra, clearinghouse/Availity)
- Insurance & Credentialing (CAQH ProView, Medicare PECOS, Medi-Cal PAVE/DHCS, plus one entry per payer portal — list all 17 payers by name)
- State Licensing & DEA (CA BreEZe, DEA Diversion Control Portal, CURES/CA PDMP)
- Business & Corporate (CA SOS BizFile, IRS EFTPS, CA FTB MyFTB, City of Torrance Business License Portal)
- Communication & Workspace (Google Workspace Admin, Google Voice, GoDaddy)
- Infrastructure & Tech (Supabase Brighter Days project, 1Password vault admin, future tech)
- Malpractice & Insurance (CAP/MPT Portal, general liability/workers comp when obtained)

For each entry include: Item name, Username field hint (e.g., "NPI or email — varies by portal"), URL, and any notes.

**Section 2: Items That Stay in Personal Vault**
Explicit exclusion list per CONTEXT.md: personal banking, SSN, personal email accounts, personal social media, personal credit cards. Explain WHY: "Personal items in the practice vault create liability exposure and complicate any future practice sale or provider addition."

**Section 3: Migration From Plaintext Documents**
Per Phase 1 audit, SSN/passwords/portal credentials are currently in plaintext Word docs (Master Key.docx, liscensing notes.docx). This section MUST include:
1. Step-by-step instructions for Valentina to create the shared vault
2. Checklist of every login/credential to migrate from Word docs into 1Password (each item = a checkbox)
3. Instruction to DELETE the plaintext documents after confirming all data is in 1Password
4. Note about verifying account plan supports shared vaults — if on Individual plan, upgrade to 1Password Families ($4.99/mo) or Teams ($19.95/mo)

**Section 4: Vault Entry Cross-Reference Table**
A table mapping each credential-seed.sql `vault_entry_ref` value to the 1Password category and entry name. This connects the Supabase data model to the physical vault organization.

**Section 5: Ongoing Maintenance**
- When a new payer portal is added: create 1Password entry in "Insurance & Credentialing" category
- When credentials are renewed: update both 1Password entry AND Supabase credentials table
- Annual audit: review all entries, confirm no plaintext credential storage elsewhere

Format as a clean markdown document. Professional tone — this is an operational document, not internal dev notes.
  </action>
  <verify>
File exists and covers all sections:
- `grep "Brighter Days Practice" .planning/phases/02-credential-vault-monitoring/1password-vault-spec.md` confirms vault name
- `grep -c "payer" .planning/phases/02-credential-vault-monitoring/1password-vault-spec.md` returns >10 (all 17 payers referenced)
- `grep "Master Key.docx" .planning/phases/02-credential-vault-monitoring/1password-vault-spec.md` confirms plaintext migration is addressed
- `grep "Personal Vault" .planning/phases/02-credential-vault-monitoring/1password-vault-spec.md` or similar confirms exclusion list exists
  </verify>
  <done>1password-vault-spec.md is a complete, step-by-step guide for organizing the "Brighter Days Practice" shared vault. All 7 categories listed with specific entries. All 17 payer portals included. Migration instructions cover plaintext Word docs. Personal vault exclusion list is explicit. Cross-reference table maps vault_entry_ref values from credential-seed.sql to 1Password entries.</done>
</task>

</tasks>

<verification>
- credential-schema.sql creates 2 tables (credentials, payer_tracker) and 2 views (credential_alert_queue, payer_credentialing_alerts) extending Phase 1 schema
- credential-seed.sql contains 12 credential rows with real numbers, proper FK links, and Medicare shown as ACTIVE
- 1password-vault-spec.md covers all 7 functional categories with all 17 payer portals and includes plaintext migration instructions
- No deferred items included (no working automation, no calendar events, no email sends)
</verification>

<success_criteria>
- Schema DDL is valid SQL that extends Phase 1 without modifying existing tables
- Every credential from the research inventory has a seed data row with real numbers
- CAQH has renewal_cycle_days = 120 and a note about verifying last attestation date
- Medicare is ACTIVE (per CONTEXT.md correction, not DEACTIVATED per Phase 1)
- 1Password spec lists all logins by functional category with migration checklist
- All vault_entry_ref values in seed data match entries in the vault spec
</success_criteria>

<output>
After completion, create `.planning/phases/02-credential-vault-monitoring/02-01-SUMMARY.md`
</output>
